{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "默认权限：窗口核心能力 + SQL、对话框、文件系统、路径插件。",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:path:default",
    "sql:default",
    "dialog:default",
    "fs:default"
  ]
}
